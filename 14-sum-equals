public Boolean sumEquals(Integer a, Integer b, Integer c) {

    int suma = a.intValue() + b.intValue();
    if (suma == c.intValue()) {
        return true;
    } else {
        return false;
    }
    
}